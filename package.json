{
  "name": "tse-recruitment",
  "version": "1.0.0",
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend",
    "start": "npm run build --prefix frontend && mv frontend/build backend/public && npm start --prefix backend"
  }
}
